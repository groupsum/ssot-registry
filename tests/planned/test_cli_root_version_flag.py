import pytest


pytestmark = pytest.mark.skip(reason="Planned SSOT coverage for root CLI --version support.")


def test_cli_root_version_flag_scope_planned():
    """Placeholder for ssot, ssot-cli, and ssot-registry root --version coverage."""
