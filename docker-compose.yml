services:
  ssot-registry_lander:
    build:
      context: .
    image: ssot-registry_lander:latest
    container_name: ssot-registry_lander
    restart: unless-stopped
    ports:
      - "${FORWARD_PORT:?FORWARD_PORT is required}:80"
