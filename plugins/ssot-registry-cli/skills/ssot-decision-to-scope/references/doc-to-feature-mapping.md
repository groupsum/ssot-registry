# Document To Feature Mapping

Before leaving this phase, make the scope mapping explicit:

- Each ADR should identify which feature IDs it governs.
- Each SPEC should identify which feature IDs it constrains or defines.
- Each feature should be linkable back to its ADRs or SPECs through notes, links, or follow-on graph edges.

If the mapping is missing, the scope is not decision-complete.
