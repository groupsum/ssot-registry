interface:
  display_name: "SSOT Feature/Test Linking"
  short_description: "Create features/tests and link verification edges."
  default_prompt: "Use $ssot-feature-test-linking to create features and tests, then link them bidirectionally with claims and evidence as needed."
