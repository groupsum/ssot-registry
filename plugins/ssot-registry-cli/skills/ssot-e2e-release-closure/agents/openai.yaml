interface:
  display_name: "SSOT E2E Release Closure"
  short_description: "Compatibility bridge for older end-to-end SSOT prompts."
  default_prompt: "Use $ssot-e2e-release-closure to route an older end-to-end SSOT request into the newer orchestrated workflow stack."
