# ssot-registry

Canonical Python runtime and behavior engine for SSOT.

- owns mutation semantics, validation, guards, snapshots, and migrations
- consumes `ssot-contracts` for canonical artifacts
- consumes `ssot-views` for derived projections
