# ssot-cli

`ssot-cli` is the primary command-line distribution for SSOT workflows.

- Install the core library with `pip install ssot-registry`
- Install the CLI with `pip install ssot-cli`
- The `ssot` command is provided by this package
- `ssot-registry` remains a compatibility command during the transition
