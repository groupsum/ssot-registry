# ssot-tui

`ssot-tui` is a Textual-based terminal UI for browsing and operating on SSOT registries.

- Install with `pip install ssot-tui`
- Depends on `ssot-registry` for all domain operations
- Targets phased delivery: navigation first, then CRUD and guided workflows
