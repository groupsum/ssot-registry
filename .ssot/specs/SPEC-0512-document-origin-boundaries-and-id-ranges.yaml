adr_ids: []
body: |-
  ## Scope
  
  This is an `ssot-core` maintainer specification for the upstream `ssot-registry` repository.
  
  ## Rules
  
  - Upstream `ssot-core` ADRs and specs SHALL be authored canonically under `.ssot/adr/` and `.ssot/specs/` in `ssot-registry`.
  - Packaged downstream templates SHALL live under `pkgs/ssot-contracts/src/ssot_contracts/templates/`.
  - Packaged template ADRs/specs MAY be governed either as official `ssot-origin` content or as trusted `extension-pack` content.
  - Repo-local ADRs/specs are authored inside downstream repositories under `.ssot/adr/` and `.ssot/specs/`.
  - Standalone packaged document repositories MAY declare `repo.kind: extension-pack`.
  
  ## Number Ranges
  
  - `ADR-0001..ADR-0599` and `SPEC-0001..SPEC-0599` are reserved for `ssot-core`.
  - `ADR-0600..ADR-0999` and `SPEC-0600..SPEC-0999` are reserved for `ssot-origin`.
  - `ADR-1000..ADR-4999` and `SPEC-1000..SPEC-4999` are reserved for repo-local downstream use.
  - `extension-pack` ADR/SPEC numbering SHALL use catalog-scoped reservations and SHALL NOT reuse repo-local downstream reservations.
  
  ## Uniqueness
  
  - ADR numbers SHALL be unique across `ssot-origin`, `ssot-core`, repo-local, and each trusted `extension-pack` reservation scope.
  - SPEC numbers SHALL be unique across `ssot-origin`, `ssot-core`, repo-local, and each trusted `extension-pack` reservation scope.
  - Upstream maintainers SHALL NOT publish core ADRs or specs that reuse an `ssot-origin` number.
  - Packaged `ssot-origin` templates SHALL NOT consume `ssot-core` or repo-local ranges.
  - Trusted `extension-pack` catalogs SHALL own their ranges through explicit reservation rows and SHALL NOT reuse `ssot-origin` or repo-local ownership semantics.
decision_date: null
id: "spc:0512"
kind: "spec"
number: 512
origin: "ssot-core"
references: []
schema_version: "0.1.0"
slug: "document-origin-boundaries-and-id-ranges"
spec_kind: "governance"
status: "draft"
status_notes: []
summary: "This is an `ssot-core` maintainer specification for upstream document-origin boundaries, including extension-pack catalogs."
superseded_by: []
supersedes: []
tags: []
title: "Document origin boundaries and ID ranges"
